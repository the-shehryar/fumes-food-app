import { ItemSize } from "@/types/type";
import SizeSelector from "../components/SizeSelector";
import { Text, View } from "react-native";

export default function SizeFinder ({sizes}: {sizes : ItemSize[]}){
    return (
        <View>
            <Text>Make me happy</Text>
        </View>
    )
}